upstream ridge_server {
    server server:3000;
}

upstream ridge_ml {
    server ml-service:8000;
}

server {
    listen 80;
    server_name _;

    # Security headers
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";

    client_max_body_size 2m;

    # Dashboard + API
    location / {
        proxy_pass http://ridge_server;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SSE alert stream
    location /api/v1/events/alerts {
        proxy_pass http://ridge_server;
        proxy_http_version 1.1;
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding off;
    }

    # ML service (internal, optional external access)
    location /ml/ {
        proxy_pass http://ridge_ml/;
        proxy_set_header Host $host;
    }
}

# Uncomment for TLS in production:
# server {
#     listen 443 ssl http2;
#     server_name ridge.example.com;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     include /etc/nginx/conf.d/ridge.conf;  # same location blocks
# }
